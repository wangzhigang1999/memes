trigger:
  paths:
    exclude:
      - README.md
      - LICENSE
      - .gitignore

resources:
  - repo: self
pool: "Azure Pipelines"

variables:
  image-repo: 'bupt2018/memes'

stages:
  - stage: BuildAndPush
    displayName: BuildAndPush
    jobs:
      - job: BuildAndPush
        displayName: BuildAndPush

        steps:
          - task: CmdLine@2
            inputs:
              script: ' x=`echo "$(Build.SourceVersion)" | head -c 8`; echo "##vso[task.setvariable variable=SHORT_COMMIT]$x"'

          - task: Docker@2
            inputs:
              containerRegistry: 'ACR'
              repository: "$(image-repo)"
              command: 'buildAndPush'
              tags: '$(Build.BuildNumber)-$(SHORT_COMMIT)'
              Dockerfile: '**/Dockerfile'



          - script: |
              curl --location --request PUT --X PUT 'http://cd.bupt.site:30303/api/v1/deployments' --header 'Content-Type: application/json'  --header 'key: xHykNahAJEdmbWNvsUjnGGyNDRwTKBhCupLsawKlBOOueNgsYWwxgSrUNFGfAvIY'    --header 'value: token-wanz-memes-memes-babdcb03'      --data '{ "namespace":"wanz","deployment":"memes","container": "memes","image": "registry.cn-beijing.aliyuncs.com/$(image-repo):$(Build.BuildNumber)-$(SHORT_COMMIT)"}'
            displayName: 'send curl'