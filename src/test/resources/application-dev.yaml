storage:
  type: local

local:
  urlPrefix: localhost:8080"

token: hello

management:
  influx:
    metrics:
      export:
        enabled: false
logging:
  level:
    root: info

spring:
  datasource:
    url: ${jdbcUrl}
    username: ${jdbcUser}
    password: ${jdbcPassword}
    driver-class-name: com.mysql.cj.jdbc.Driver