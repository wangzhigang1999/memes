spring:
  application:
    name: memes
  profiles:
    active: ${env}

  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 3MB
  web:
    resources:
      static-locations: [ "classpath:/static/","file:./memes/" ]
  webflux:
    static-path-pattern: /static/**

